import { TbBrandReactNative } from "react-icons/tb";

type Props = {
  className?: string;
};

export default function ReactNativeIcon({ className }: Props) {
  return <TbBrandReactNative className={className} />;
}
