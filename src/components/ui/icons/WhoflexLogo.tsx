import WLogo from "../../../../public/images/whoflex-logo.svg";

type Props = { className?: string };

export default function WhoflexLogo({ className }: Props) {
  return <WLogo className={className} />;
}
